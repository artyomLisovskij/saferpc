{
    "name": "hardhat-fork",
    "version": "1.0.0",
    "devDependencies": {
      "hardhat": "^2.16.0",
      "@nomiclabs/hardhat-ethers": "^2.2.0"
    },
    "scripts": {
      "node": "cross-env-shell hardhat node --hostname 0.0.0.0 --port 8545 --fork $RPC_URL"
    }
}